<%- include('../partials/head') %>

    <div class="container mt-4 text-center">
        <h1 class="mb-4">Explore Our Community</h1>

        <% users.forEach(user=> { %>
            <a href="/users/<%= user._id %>" class="btn btn-primary d-block mb-2">
                <%= user.username %>
            </a>
            <% }) %>

    </div>

    <%- include('../partials/footer') %>